section.contact_me.sections#contact
    .contact_content.content
        h1 Shoot me a message
        p Have an amazing idea or project, something related to technology that excites me or have an offer!!
    form(onsubmit = "formHandler(e)").contact_form.container
        input.input#firstName(type="text" name ="firstName" placeholder = "First name*" required)
        input.input#lastName(type="text" name ="lastName" placeholder = "Last name")
        input.input#email(type="email" name ="email" placeholder = "your email*" required)
        textarea.input#textarea(name="textarea", cols="30", rows="10" placeholder = "Your message here!!" required)
        button(type="submit").form_button#formButton
            a Send message
